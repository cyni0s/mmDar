# Pinned pip dependencies for mmDar / RadarHD
# torch, numpy, tensorboard are provided by the NGC base image (nvcr.io/nvidia/pytorch:25.02-py3)
# Pin numpy<2 to prevent scipy/open3d from upgrading past NGC-bundled version.
numpy<2.0

torchinfo>=1.8.0
gdown
matplotlib>=3.8.0
pandas>=2.0.0
open3d>=0.18.0
opencv-python>=4.0.0
pillow>=10.0.0
scipy>=1.12.0
