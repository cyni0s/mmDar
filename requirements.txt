# Pinned pip dependencies for mmDar / RadarHD
# torch, numpy, tensorboard are provided by the NGC base image (nvcr.io/nvidia/pytorch:25.02-py3)
# Do NOT add torch or numpy here — they conflict with the NGC-managed versions.

torchinfo>=1.8.0
gdown
matplotlib>=3.8.0
pandas>=2.0.0
open3d>=0.18.0
opencv-python>=4.0.0
pillow>=10.0.0
scipy>=1.12.0
